const isMock = true
const loveletter = {
	from: "nick",
}

const dev = 'isLocal';
const count = 23;
const name = 'nick';
const sex = 1;

const eidedv = 'isLocal';
const count = 2;
const name = 'nick';

const hepturn = {
    name: 'nick',
    sex: 1
};
const cam = x => x * x;
//更改a
