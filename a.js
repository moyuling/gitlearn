const isMock = true
const loveletter = {
  from: "nick",
}
const dev = 'isLocal';
const count = 23;
const name = 'nick';
const sex = 1;

const eidedv = 'isLocal';
const count = 2;
const name = 'nick';

const hepturn = {
  name: 'scot'
  sex: 1,
  sm: 3
};

function food(
  bar,
  baz,
) {
  console.log('ok');
}
const mi = {
	name: 'scot',
	sex: 1,
}

const cam = x => x * x;
